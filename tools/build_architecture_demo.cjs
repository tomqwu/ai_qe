const esbuild=require('esbuild'),fs=require('node:fs'),path=require('node:path');
(async()=>{const out=path.join(__dirname,'../assets/js/architecture-3d.js');const result=await esbuild.build({entryPoints:[path.join(__dirname,'architecture-demo/main.js')],bundle:true,minify:true,format:'esm',target:['es2022'],legalComments:'eof',write:false});const bytes=result.outputFiles[0].contents;if(process.argv.includes('--check')){if(!fs.existsSync(out)||!Buffer.from(bytes).equals(fs.readFileSync(out)))throw new Error('3D bundle is stale: node tools/build_architecture_demo.cjs');console.log('3D browser bundle matches source');}else{fs.writeFileSync(out,bytes);console.log('3D browser bundle:',bytes.length,'bytes');}})().catch(e=>{console.error(e);process.exitCode=1});
